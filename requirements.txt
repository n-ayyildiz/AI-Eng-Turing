# Core framework
streamlit==1.39.0

# LangChain ecosystem (pinned to 0.3.x — do not upgrade to 0.4.x,
# which breaks langchain-chroma and langchain-openai compatibility)
langchain==0.3.25
langchain-openai==0.2.8
langchain-community==0.3.24
langchain-chroma==0.1.4
langchain-core==0.3.63
langchain-text-splitters==0.3.8
langsmith==0.1.147

# Vector store + embeddings
chromadb==0.5.18

# PDF loading & processing
pypdf==5.1.0

# Keyword / sparse retrieval
rank-bm25==0.2.2

# Numerical computing (cosine similarity, temporal tagging)
numpy>=1.26.0

# Token counting & cost tracking
tiktoken==0.8.0

# Environment variables
python-dotenv==1.0.1

# PDF export
reportlab==4.2.5

# PubMed integration (NCBI E-utilities via requests)
requests==2.32.3
xmltodict>=0.13.0

# Evaluation — RAGAS + benchmark runner
ragas==0.2.6
datasets==3.1.0
pandas==2.2.3
